version: 2

models:
  - name: dim_products
    columns: 
      - name: product_id
        tests:
          - unique_counts
  - name: dim_users
    columns: 
      - name: user_id
        tests:
          - unique_counts