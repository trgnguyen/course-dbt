version: 2

models:
  - name: fact_user_order
    description: this table shows the statistics and performance of our customers
    columns:
      - name: user_id

      - name: first_name

      - name: last_name

      - name: first_order

      - name: last_order

      - name: orders_cnt

      - name: product_types_cnt

      - name: product_quantity

      - name: order_cost

      - name: shipping_cost

      - name: discount_amount

      - name: total_spend